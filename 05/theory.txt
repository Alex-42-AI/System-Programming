Пренасочване на вход, изход и изход за грешки
close({0|1|2})
open(file)

open(file)
close({0|1|2})
dup(file)
close(file)

open(file)
dup2(file, {0|1|2})
close(file)
"Приспиване" на процес
#include <unistd.h>
unsigned int sleep(unsigned int seconds) - "приспива" процеса
