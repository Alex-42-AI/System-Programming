Write a C program, that implements command cp file1 cile2
Write a C program, that implements command head file
Write a C program, that implements command cmp -s file1 file2
Write a C program, that implements command wc file
Write a C program, that implements command tail file
Write a C program, that implements command cut -cm-n file
Write a C program, that implements command fgrep text file (file lines are up to 80 symbols)
